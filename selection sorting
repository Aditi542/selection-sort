#include<stdio.h>
void main()
{ 
    int a[10],i,j,t;
    printf("Enter Array"); // input 
    for(i=0;i<10;i++)
{
    scanf("%d",&a[i]);
}
    for(i=0;i<9;i++)
{ 
    for(j=i+1;j<10;j++)
{ 
    if(a[j]<a[i])
{  
    t=x[i];
    x[i]=x[j];
    x[j]=t;
   }
  }
}
   printf("After sorting");
   for(i=0;i<10;i++)
   printf("%d",a[i]);
}
